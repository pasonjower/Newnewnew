# Output artifacts

Running `python scripts/demo_runner.py` populates this folder with:

- `replenish_now.csv`
- `cut_defer.csv`
- `double_down.csv`
- `draft_purchase_orders.csv`
- `kpi_summary.json`

These are the same payloads Retool will expose via tables, tiles, and downloads.
